spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:test_ts_db
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true
    properties:
      hibernate:
        format_sql: false

tasks:
  page:
    size: 3